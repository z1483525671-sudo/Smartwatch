#ifndef __ESP8266_H
#define __ESP8266_H

#include "main.h"
#include <stdbool.h>

/* ????(??????????) */
#define ESP_RST_PORT        GPIOB      // RST ????
#define ESP_RST_PIN         GPIO_PIN_5 // RST ??

/* ?? CH_PD ????? 3.3V,?????? GPIO ??,?????????,??? */
// #define ESP_CH_PD_PORT   GPIOB
// #define ESP_CH_PD_PIN    GPIO_PIN_6

/* ???? */
void ESP8266_Init(uint32_t baudrate);          // ???(????115200)
void ESP8266_Rst(void);                        // ????
bool ESP8266_SendCmd(const char *cmd, const char *ack, uint32_t timeout_ms); // ??AT???????
bool ESP8266_JoinAP(const char *ssid, const char *pwd);   // ?? WiFi
void ESP8266_GetIP(void);                      // ????? IP
bool ESP8266_StartTCPServer(uint16_t port);    // ?? TCP ???(?????)
void ESP8266_Process(void);                    // ??????,??????????

#endif